{
  "version": 2,
  "builds": [
    {
      "src": "apps/admin/project.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist/apps/admin"
      }
    }
  ],
  "buildCommand": "npx nx build admin --prod",
  "outputDirectory": "dist/apps/admin"
}
