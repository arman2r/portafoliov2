import { SingleChartInputDirective } from './Single-chart-input.directive';

describe('SingleChartInputDirective', () => {
  it('should create an instance', () => {
    const directive = new SingleChartInputDirective();
    expect(directive).toBeTruthy();
  });
});
